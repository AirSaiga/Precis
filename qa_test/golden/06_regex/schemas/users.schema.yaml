version: 2

id: users
name: users
description: 黄金案例 06 正则约束测试表

source:
  mode: relative_file
  path: data/users.csv

columns:
  - id: id
    name: id
    type: integer
    primary_key: true
    nullable: false
  - id: email
    name: email
    type: string
