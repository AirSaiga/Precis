version: 2

id: orders
name: orders
description: 黄金案例 12 Transform DAG 测试表（status 经 UpperCase 转换为大写）

source:
  mode: relative_file
  path: data/orders.csv

columns:
  - id: order_id
    name: order_id
    type: string
    primary_key: true
    nullable: false
  - id: status
    name: status
    type: string
  - id: amount
    name: amount
    type: float
