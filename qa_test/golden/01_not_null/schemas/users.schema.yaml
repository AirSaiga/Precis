version: 2

id: users
name: users
description: 黄金案例 01 非空约束测试表

source:
  mode: relative_file
  path: data/users.csv

columns:
  - id: id
    name: id
    type: integer
    primary_key: true
    nullable: false
  - id: name
    name: name
    type: string
  - id: age
    name: age
    type: integer
