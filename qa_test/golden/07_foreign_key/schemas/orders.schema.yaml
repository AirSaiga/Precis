version: 2

id: orders
name: orders
description: 黄金案例 07 外键约束-子表

source:
  mode: relative_file
  path: data/orders.csv

columns:
  - id: id
    name: id
    type: string
    primary_key: true
    nullable: false
  - id: user_id
    name: user_id
    type: string
  - id: amount
    name: amount
    type: float
