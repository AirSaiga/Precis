version: 2

id: users
name: users
description: 黄金案例 08 条件约束测试表

source:
  mode: relative_file
  path: data/users.csv

columns:
  - id: id
    name: id
    type: integer
    primary_key: true
    nullable: false
  - id: age
    name: age
    type: integer
  - id: is_adult_only
    name: is_adult_only
    type: boolean
