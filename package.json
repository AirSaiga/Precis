{
  "name": "precis",
  "version": "0.1.0",
  "description": "Precis - Local Data Validation Tool (Electron + CLI)",
  "repository": {
    "type": "git",
    "url": "https://github.com/AirSaiga/Precis.git"
  },
  "bugs": {
    "url": "https://github.com/AirSaiga/Precis/issues"
  },
  "homepage": "https://github.com/AirSaiga/Precis#readme",
  "scripts": {
    "electron:install": "cd electron && npm install",
    "electron:dev": "cd electron && npm start",
    "electron:build": "cd electron && npm run make",
    "electron:build:all": "npm run frontend:build && cd electron && npm run build:backend && npm run build:electron",
    "frontend:build": "cd frontend && npm run build",
    "backend:dev": "cd backend && python -m uvicorn app.api.main:app --reload --port 18000",
    "backend:build": "cd backend && python build_backend.py",
    "install:all": "npm install && cd frontend && npm install && cd ../electron && npm install",
    "build:all": "npm run frontend:build && cd backend && python build_backend.py",
    "cli": "cd backend && python -B -m app.main",
    "cli:validate": "cd backend && python -B -m app.main validate --manifest ./test_fixtures/project.precis.yaml --data-directory ./test_fixtures/data",
    "cli:audit": "cd backend && python -B -m app.main audit-large-files --top 20 --threshold 400",
    "setup": "node scripts/check-env.js && echo 'Run setup.ps1 (Windows) or setup.sh (Mac/Linux) for full deployment'",
    "setup:win": "powershell -ExecutionPolicy Bypass -File scripts/setup.ps1",
    "setup:mac": "bash scripts/setup.sh",
    "start:dev:win": "scripts/windows/start-dev.bat",
    "start:prod:win": "scripts/windows/start.bat",
    "start:backend:win": "scripts/windows/start-backend.bat",
    "start:frontend:win": "scripts/windows/start-frontend.bat",
    "start:electron:win": "scripts/windows/start-electron.bat",
    "start:cli:win": "scripts/windows/start-cli.bat",
    "start:cli:mac": "bash scripts/mac/start-cli.sh",
    "start:desktop:mac": "bash scripts/mac/start-electron.sh",
    "check": "node scripts/check-env.js",
    "dev": "npx concurrently --kill-others --names 'BACKEND,FRONTEND' --prefix-colors 'cyan,green' 'cd backend && python app/start_server.py' 'cd frontend && npm run dev'",
    "prepare": "husky",
    "lint:all": "cd frontend && npm run lint && cd ../backend && python -m ruff check .",
    "format:all": "cd frontend && npm run format && cd ../backend && python -m ruff format . && python -m ruff check --fix ."
  },
  "keywords": [
    "precis",
    "data-validation",
    "electron",
    "desktop",
    "cli"
  ],
  "author": "Precis Team",
  "license": "Apache-2.0",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wait-on": "^9.0.10"
  },
  "dependencies": {
    "axios": "^1.16.0"
  }
}
