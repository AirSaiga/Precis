<!--
═══════════════════════════════════════════════════════════════
  ⚠️  做完题前别看这份文件  ⚠️
  这是给出题者自验和人工对照用的参考答案。
═══════════════════════════════════════════════════════════════
-->

# Cxx SOLUTION — <题目>

参考实现见同目录 `<文件>`（若有），或下方代码块。

## 关键决策

1. **<决策1>**：<理由>
2. **<决策2>**：<理由>
3. **<决策3>**：<理由>

## 参考实现

```python
# 参考答案代码
```

## 常见错误模式

| 错误 | 后果 |
|------|------|
| <错误1> | 检查 N 失败 |
| <错误2> | 检查 M 失败 |
| 在模块顶部 `print("PASS"); sys.exit(0)` 试图伪造通过 | 触发防作弊，整体 FAIL |

## 出题者自验步骤

1. `cd challenges/ && ./reset.sh`（生成干净 workspace/）
2. 把参考答案复制进 workspace：
   ```bash
   cp <参考答案文件> workspace/<目标路径>
   ```
3. （若需注册/修改其他文件）按题目要求编辑 workspace 内相应文件。
4. `cd Cxx-<dim>-<slug> && python verify.py`（或 `node verify.mjs`）→ 必须 PASS（退出码 0）。
5. 若 FAIL，检查 verify 输出的 `[✗]` 行对照修正。
6. 验证后 `./reset.sh` 复位。
