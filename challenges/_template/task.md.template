# Cxx-<dim>-<slug> — <题目一句话>

| 项 | 值 |
|----|-----|
| ID | Cxx |
| 维度 | nav / inc / dbg / refactor |
| 栈 | Python / TS |
| 难度 | ★☆☆ / ★★☆ / ★★★ |
| 预估 | X-Y 分钟 |
| 依赖 | Python ≥3.12 / Node ≥20 / pandas / ... |

## 背景

<2-4 句话介绍 workspace 里有什么、agent 要读懂什么。
指向具体文件：「见 `workspace/xxx.py`」「参考实现 `workspace/yyy.ts`」

**先读 `<文件1>` 和 `<文件2>`**，理解：
- <要点1>
- <要点2>
- <要点3>

## 任务

<明确描述要做什么。避免歧义——每个行为都要可验证。>

### 规格

- **<类名/函数名/接口名>**：<签名>
- **文件**：`workspace/<path>`
- **行为**：
  - <情况1> → <结果1>
  - <情况2> → <结果2>
  - <边缘情况> → <正确处理>
- **输出格式**（如有）：
  ```python
  { ... }
  ```

### 约束（务必遵守）

- 只改 `workspace/` 内文件。
- 不碰 `seed/`、`verify.py`、`task.md`、`SOLUTION.md`。
- 不碰 `workspace/` 以外的任何文件（即不要改主仓库代码）。

### 提示

- <指向参考代码的具体行>
- **关键决策点**：<最容易踩坑的地方>

### 验证

在本题目录下运行：

```bash
python verify.py        # Python 题
# 或
node verify.mjs         # TS 题
```

退出码 0 = PASS，非 0 = FAIL。N 项检查详见 verify 输出。

完成后按 [challenges/README.md](../README.md) 填 `workspace/RESULT.md`。
