# workspace/ 是 agent 运行时工作区，由 reset 脚本从 seed/ 复制，不入库
C*/workspace/

# RESULT.md 是 agent 跑完填的结果产物，不入库（在 workspace 内已被上一行覆盖，此处显式提示）
C*/workspace/RESULT.md

# results/ 入库（报告是评测产出，要留档）——不忽略

# SOLUTION.md 不入 git（防 worktree 评测泄露；本地保留供出题者参考）
# 原理：git rm --cached 后 worktree 从 git checkout 天然不含答案
C*/SOLUTION.md
R*/SOLUTION.md
X*/SOLUTION.md

# 评测结果留存在各 eval worktree（worktree 即存档）；主仓库 results/ 只落 report.py
# 生成的 LEADERBOARD.md 派生产物（逐题结果不拷回、不入库）
results/*
!results/LEADERBOARD.md

# C01 的独立参考答案文件（同上）
C01-nav-add-maxlength/maxlength_constraint.py
